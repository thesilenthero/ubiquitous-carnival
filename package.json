{
  "name": "job-search-tracker",
  "version": "2.0.0",
  "private": true,
  "description": "Single-user job application tracking dashboard with live analytics. Python/FastAPI backend + Vite/React frontend.",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "python3 -m uvicorn server.main:app --reload --port 4000",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web install && npm --prefix web run build",
    "start": "python3 -m uvicorn server.main:app --host 0.0.0.0 --port \"${PORT:-4000}\"",
    "lan": "sh scripts/lan_url.sh && npm start",
    "seed": "python3 -m server.seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
